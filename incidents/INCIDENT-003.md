# INCIDENT-003 – Transaction Search Performance

**Priority:** P2

Operations reports that transaction investigation becomes slow as transaction volume increases. Some searches take several seconds and are expected to deteriorate further.

## Your task
Generate enough synthetic data to make database access patterns meaningful (target ~50,000 transactions or more), identify a slow query/access pattern, collect evidence, improve it, and document before/after results in `investigation/INCIDENT-003-RCA.md`.
